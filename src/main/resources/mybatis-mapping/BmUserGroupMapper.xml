<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis_mapper.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhsj.dao.BmUserGroupDao">

    <!-- add -->
    <insert id="add">
       insert into bmUserGroup (
          bmUserId,
          userGroupId
       ) values (
          #{bmUserId},
          #{userGroupId}
       )
    </insert>

    <!-- delete -->
    <delete id="delete">
       delete from bmUserGroup where id = #{id}
    </delete>
    
    <!-- update -->
    <update id="update">
      update bmUserGroup set
           bmUserId = #{bmUserId},
           userGroupId = #{userGroupId}
           where id = #{id}
    </update>

    <!-- 通过用户id查询关联的用户组 -->
    <select id="getBmUserGroupByBmUserId" resultType="com.zhsj.model.BmUserGroup">
            select * from bmUserGroup where bmUserId = #{bmUserId}
    </select>
</mapper>