<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis_mapper.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhsj.dao.BusinessInfoDao">

         <select id="getBusinessInfoById" parameterType="java.lang.Integer">
             select * from businessinfo where id = #{id}
         </select>
    
         <insert id="insert" useGeneratedKeys="true" parameterType="com.zhsj.model.BusinessInfo" >
            insert into businessinfo (
              shopName,
              parentId,
              shopType,
              areaId,
              address,
              contact,
              contactPhone,
              shopLogo,
              intro,
              status
            ) values (
              #{shopName},
              #{parentId},
              #{shopType},
              #{areaId},
              #{address},
              #{contact},
              #{contactPhone},
              #{shopLogo},
              #{intro},
              #{status}
            )
         </insert>
         
         
         <update id="update" >
         
             update businessinfo set 
                    shopName=#{shopName},
                    parentId = #{parentId},
                    shopType = #{shopType},
                    areaId = #{areaId},
                    address = #{address},
                    contact = #{contact},
                    contactPhone = #{contactPhone},
                    shopLogo = #{shopLogo},
                    intro = #{intro},
                    status = #{status}
                    where id = #{id}
         
         </update>



        <select id="getBusinessInfoList" parameterType="java.lang.Integer" >
           select * from businessinfo limit #{start},#{end}
        </select>
        
        
        <delete id="delete" parameterType="java.lang.Integer">
           delete from businessinfo where id = #{id}
        </delete>
</mapper>