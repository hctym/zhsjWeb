<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis_mapper.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhsj.dao.UserGroupDao">

    <!-- 查询-->
    <select id="getList" resultType="java.util.HashMap">
        select id,name,isValid,ctime from userGroup
    </select>
    
    <!-- 添加 -->
    <insert id="add" parameterType="com.zhsj.model.UserGroup">
       insert into userGroup (
	         name,
	         isValid,
	         utime,
	         ctime
       ) values (
	         #{name},
	         #{isValid},
	         #{utime},
	         #{ctime}
       )
    </insert>
</mapper>